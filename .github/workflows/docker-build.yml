name: Docker Build

on:
  pull_request: ~

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build with docker compose
        run: docker compose build